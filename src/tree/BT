  // in binary tree we have left and right child.
        // Class Node{
        // int data;
        // Node Left;
        // Node Right;
        // }
        // Size of the tree is the number of nodes in the tree.
        // There is no regular order in which the nodes are stored in the tree.
        // Siblings are the nodes that have the same parent.
        // The height of the tree is the maximum number of the nodes along the longest path from the root node to a leaf node.
        // We find the height of the tree by traversing the tree and counting the number of nodes along the longest path from the root node to a leaf node.
        // To find the height of any node, we need to traverse the tree from that node to the leaf node and the maximum height is the max of all the node in the path to the leaf node.
        // types of a binary tree.
        // complete binary tree: every node has 2 child. if the node is the root, then it has no child. every level of the tree is filled, and the last level is filled from left to right.
        // full binary tree: every node has either 0 or 2 child. if the node is the root, then it has no child. every level of the tree is filled, and the last level is filled from left to right.
        // perfect binary tree: every node has either 0 or 2 child. if the node is the root, then it has no child. every level of the tree is filled, and the last level is filled from left to right.
        // height balanced binary tree: every node has either 0 or 2 child. if the node is the root, then it has no child. every level of the tree is filled, and the last level is filled from left to right.
        //skewed binary tree: every node has only one child.